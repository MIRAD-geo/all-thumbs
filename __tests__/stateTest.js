// import reducer, {checkOut, addItem, removeItem, updateQuantity} from '../client/Slices/cartSlice'

// test('should return the initial state', () => {
//     expect(reducer(undefined, {})).toEqual(
//         {
//             items: []
//         }
//     )
// })

// test('should handle a new item added to empty cart', () => {
//     const previousState = [];
//     expect(reducer(previousState, addItem('1'))).toEqual(
//         {
//             items:['1']
//         }
//     )
// })